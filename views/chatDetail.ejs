<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="/main.css" rel="stylesheet">
</head>
<body class="grey-bg">
    <%- include('nav.ejs') %>

    <!-- <%= JSON.stringify(data[1]) %> -->
    <div class="detail-bg">
        <h4>
            채팅
        </h4>
        <!-- object끼리의 비교는 메모리주소를 비교함 그러므로 안의 내용이 같아도 메모리 참조가 다르므로 false가 나옴 -->
        <div class="chat-screen">
            <% for(var i = 0; i< data[1].chatContent.length; i++){ %>
                <% if( JSON.stringify(data[1].chatContent[i].send_user) == JSON.stringify(data[0].chatRoomInfo.member[0]) ){ %>
                    <div class="chat-box mine"><span><%= data[1].chatContent[i].content %></span></div>
                <% }else{ %>
                     <div class="chat-box"><span><%= data[1].chatContent[i].content %></span></div>
                <% } %>

            <% } %>
        </div>

        <div class="chat-form">
            <input type="text" name="content" class="chat-input">
            <button type="button" class="chat-button">전송</button>
        </div>

    </div>

    <!-- 실시간 통신을 하고 싶은 페이지에 라이브러리 설치 -->
    <script src="https://cdn.jsdelivr.net/npm/socket.io@4.7.2/client-dist/socket.io.min.js"></script>
    <script>
        const socket = io() //--웹소켓 연결
        //socket.emit('ask-join','1') //--'ask-join'이라는 이름으로 '1'을 보냄

        document.querySelector('.chat-button').addEventListener('click',(e)=>{
            let content = document.querySelector('.chat-input').value; //--채팅내용
            let roomNum = "<%= data[0].chatRoomInfo._id%>" //--룸 내용
            socket.emit('chat',{content: content, room: roomNum})
            
        })

        socket.on('data',(data)=>{
            // var onDiv = document.createElement('div')
            // var onSpan = document.createElement('span')
            // onSpan.innerHTML = data.content
            // onDiv.appendChild(onSpan)
            // onDiv.classList.add('chat-box')
            // if(data.send_user) onDiv.classList.add('mine') //--공백을 넣으면 안됨
            if(data){
                document.querySelector('.chat-screen').insertAdjacentHTML('beforeEnd',
                    `<div class="chat-box"><span>${data.content}</span></div>`
                )
                document.querySelector('.chat-input').value = ''
            }
        })

        //--서버가 보낸 데이터
        // socket.on('broadcast',(data)=>{
        //     console.log(data)
        // })
    </script>

</body>
</html>